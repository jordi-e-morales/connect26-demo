# Archivo — plan anterior, NO vigente

Archivado el 2026-09-13.

- `guion/`: app de ensayo del primer plan, con agentes y eventos de seguridad
  simulados en la laptop. El plan vigente prohíbe el modo simulación: toda la
  inferencia y todos los eventos deben ser reales.
- `walkthrough.md`: narrativa inicial de la sesión (servidores UCS C225 con
  L4). El hardware y el caso de uso cambiaron.

`guion/.venv` se movió junto con la carpeta y ya no funciona desde esta ruta.
Se puede borrar sin perder nada: se regenera con `guion/requirements.txt`.

**Plan vigente:** `CLAUDE.md` del repo `agntcy-mortgage-demo-python`.
